const metaDataKey = Array(16).join(Math.random().toString(36).slice(2, 18)).slice(0, 16)
const valueKey = Array(16).join(Math.random().toString(36).slice(2, 18)).slice(0, 16)

export { metaDataKey, valueKey }
